---
layout: base
---

<div class="book-container">
  <header class='book' data-currentSpread='3'>
    <div class="spread">
      <div class="page recto cover">
        <h1 class='book-title'>Setting the Table</h1>
      </div>
    </div>
    {% for dish in collections.recipe %}
    <div class="spread">
      <div class="page verso">
        <img class='recipe-image' src="{{dish.data.thumb | prepend: "/assets/dishes/" | url}}" alt="">
        <span class="page-number">{{forloop.index | times: 2 | minus: 1}}</span>
      </div>
      <div class="page recto">
        <article class="recipe">
          <h2 class="recipe-title">
            {{dish.data.title}}
          </h2>
          <ul class="recipe-ingredients">
            {% for ingredient in dish.data.ingredients %}
            <li class='recipe-ingredient'>{{ingredient}}</li>
            {% endfor %}
          </ul>
          <div class="recipe-content">
            {{dish.templateContent}}
          </div>
        </article>
      </div>
    </div>
    {% endfor %}
    <div class="spread">
      <div class="page backcover verso">
      </div>
    </div>
  </header>
</div>